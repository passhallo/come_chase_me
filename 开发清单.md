# 《Come Chase Me》开发清单

> 竖屏微信小游戏 · 武侠雨夜 · MVP 简版（版本 A）
> 说明：「完成」= 已通过 Cocos 编辑器预览验证；微信构建 / 真机 / 音频 / 广告均未验证。

## 当前阶段

MVP 核心玩法闭环 + 角色 2D 骨骼动画（跑步/相位/受击）+ 闪避残影（对象池）均已完成并通过预览验证。下一步是 A3-6 剑气攻击与 A4 微信构建/真机测试。

## 进度

### 已完成 ✅
- [x] A3-1 场景与 UI：720×1280，Canvas / World / Player / AttackLayer / EffectLayer / HUD / ResultPanel，开始与重开可用
- [x] A3-2 前进视觉：石板循环滚动 + 雨雾 + 脚下阴影
- [x] A3-3 相位闪避：点击 0.25s 无敌 + 0.7s 冷却，HUD 显示冷却
- [x] A3-4 前方暗器攻击：预警→接近→命中→离场，未躲必结算，可重开
- [x] A3-5 完美闪避 + 子弹时间：0.08s 窗口、0.15s 慢放 0.4 倍、金色残影、连击 +1，慢放可恢复不叠加
- [x] 角色美术（详细武侠造型）：从 `temp/mcp-captures/parts/` 高清 13 部件（深蓝劲装 + 披风 + 佩剑）重切为 12 部件，按关节重算锚点（关节居中 = anchor 0.5,0.5）+ 统一缩放装配，替换原几何占位图，预览确认为完整造型（旧占位图已弃用）
- [x] 角色 2D 骨骼动画（分层 PNG + 节点父子 + Cocos Animation）：原地跑步循环（0.55s）、相位（PhaseStart 0.08s → PhaseLoop 0.17s，上移 20px / 半透明 45% / 青白染色）、受击（0.35s，后仰停住），已通过预览验证
- [x] Run 跑步动画修正（v2）：原为左右（Z-yaw）「开合跳」，已改为前后（X-pitch）俯仰 + 常量 Z（yaw）收拢——大腿 ±13°(Z=±1)、小腿膝弯 16°(Z=±2)、上臂 ±9°(Z=±8)、前臂 12±6°(Z=±8)、头 Z=0、披风/剑鞘 ±4.7°、Root 上下 ±4.5px；运行时实测脚/手横向 X 位移为 0（恒定 span=0），Y 上下 7.4-8.6px，脚距 45.8px≈肩宽（自然步姿，非开合跳），确认「朝屏幕上方向上跑」。生成器 `temp/gen_run_anim_v2.js`
- [x] 闪避残影（对象池）：普通 3 个青白残影 / 完美 5 个金白残影 + 环形冲击波 + PERFECT 文字，连续 30 次闪避无内存增长，已通过预览验证
- [x] 步频-速度绑定（GaitConfig）：新建 `assets/scripts/core/GaitConfig.ts`（baselineStrideDistance=165 / 基准 300px/s / 0.55s / 落脚比例 [0,0.5]），WorldScroller.speed 即 worldSpeed，PlayerAnimator 每帧设 Run `AnimationState.speed = worldSpeed/300`（runDuration = 165/worldSpeed），落地事件（t=0 左脚 / t=0.275 右脚 / t=0.55 回到左脚）+ 自动 HUD 调试标签；实测 240/300/360→0.8/1.0/1.2、每循环地面恒 165px、落地交替正确（修正：右脚落地原误存为比例 0.275，实为 0.5 周期=0.275s）
- [x] Run 跑步动画四关键姿势重写（v3）：弃用 v2 的「所有肢体同一正弦摆动」，改为 4 个明确关键姿势（左脚接触→身体下沉/经过→右脚接触→身体下沉/经过，t=0/0.1375/0.275/0.4125，余弦插值 17 帧）；前后迈步改由**少量 Y 位移 + Y 缩放 + 小幅旋转**组合驱动（关节居中锚点下纯旋转只产生对称缩短，故 Y 位移为主）：Root 上下 ±9px（3.6% 身高）、左右重心 ±5px，小腿 Y 位移 ±8px（脚上下 16px）+ scale.y 0.93/1.07，大腿 14°、膝弯 25°、上臂 18°、前臂 Y 位移 ±6px + scale 0.97/1.03，对侧摆臂（左脚前↔右臂前），披风/剑鞘滞后 0.05s；生成器 `temp/gen_run_anim_v3.js`。已逐帧验证 4 姿势各 transform 精确值、240/300/360 步频同步、落地事件对齐 t=0.275
- [x] 侧面跑步参考视频分析（Request E）：`temp/mcp-captures/side_run_reference.mp4` 可读（768×1344 / 24fps / 5.17s / 124 帧），用数值信号分析（背景中值分割→前景 mask→自相关+FFT）测得**真实完整周期 ≈0.72–0.74s**（非 0.55s；autocorrelation cy=0.708s、FFT 1.355Hz→0.738s，单次落脚间隔 ~0.36s）；4 关键姿势确认：接触=身体高位、经过中点=身体下沉、对侧摆臂、膝弯、脚踝前后迈步（非 V 字/横向）、披风/剑鞘滞后 0.04–0.06s。**结论：视频证实 v3 姿势设计已完全正确，无需改动画代码**；游戏基准周期仍保留 0.55s（Step 4 速度绑定约束：runDuration=165/worldSpeed，视频仅用于姿势/相位，不复制水平移动）。三禁止已遵守（未用 VideoPlayer / 未贴视频到角色 / 未覆盖角色图片），未调用私有 Editor API/鼠标模拟。4 张关键姿势截图落盘 `temp/mcp-captures/run_pose_{1..4}_*.png`（左接触/右接触/经过1/经过2，~128KB 非空白）。**遗留 caveat**：`assets/art/themes/wuxia/player/` 无马尾/衣摆独立节点（马尾烘焙进 head、衣摆烘焙进 torso），无法单独滞后

### 下一步 ⏭️
1. （此前指示暂停新增功能，暂缓）A3-6 左/右横切剑气 + 难度曲线
2. A4 MVP 测试清单：连续 10 局无内存增长、微信开发者工具构建、真机预览

### 已知问题 ⚠️
- Run 跑步动画在「MCP 重启后的预览」中**载入时不自动播放**（`playRun()` 已执行但 `isPlaying:false`），需手动 `anim.play('Run')`。原始 Z-yaw 版本同样如此，疑为 MCP 重启环境的预览加载问题而非动画回归——正式构建/真机需确认是否复现。
- 预览控制台曾报 `[PreviewInEditor] getComponent: Type must be non-nil`：已定位为**注入式诊断脚本**（`webContents.executeJavaScript` / MCP `execute_javascript`）在预览运行时调用 `node.getComponent(cc.UIOpacity)` / `cc.UITransform` 所致——预览全局 `cc` **不暴露** `UIOpacity`/`UITransform`（`typeof` 为 `undefined`），引擎 `getConstructor(undefined)` 记 `errorID(3807)` 并返回 null（不抛异常）。**项目自身代码无此问题**（9 处 `getComponent` 均用 `import {…} from 'cc'` 显式导入）；干净重载预览与 MCP `get_scene_info`/`get_hierarchy` 均 0 报错。
- 预览控制台曾持续报 `Module [Operation] does not exist.(Method: _emitMouseEvent / _emit)`：**编辑器/MCP 自动化通信错误**，非游戏逻辑。根因：此前自动化脚本试图通过私有 Editor API `Operation._emitMouseEvent` 模拟鼠标点击/移动注入预览窗口，而 `Operation` 模块在预览进程不存在。处理：停止所有鼠标事件模拟/私有 Editor API 调用，改用公开 MCP 工具（读取 + `execute_javascript` 状态检查）验证。验证：project.log 中该错误最后一次出现在 `2026-8-16 06:47:23`（第 5737 行），其后日志持续写入至 06:55:17 仅新增 MCP RPC 握手日志、**0 条**新错误。注意：当前 MCP **无公开预览点击能力**，点击类交互（相位闪避/开始按钮）仍需在预览窗口手动测试（自动输入未验证）。
